# Συνεισφορά

Ο ιστότοπος του μαθήματος είναι ένα Jekyll site που το GitHub Pages χτίζει αυτόματα
σε κάθε push στο `main`. Δεν υπάρχει βήμα build που να τρέχει με το χέρι: γράφετε
Markdown, στέλνετε PR, και η σελίδα ανανεώνεται.

Το υλικό των **εργαστηρίων** δεν βρίσκεται εδώ — ζει στο
[progintro/lab-material](https://github.com/progintro/lab-material) και έχει δικό του
[CONTRIBUTING.md](https://github.com/progintro/lab-material/blob/main/CONTRIBUTING.md).
Οι δύο ιστότοποι μοιράζονται σκόπιμα την ίδια παρουσίαση (δείτε παρακάτω).

## Δομή

| Διαδρομή | Περιεχόμενο |
| --- | --- |
| `README.md` | Η αρχική σελίδα — για τη σειρά των ενοτήτων δείτε «Σειρά των ενοτήτων» |
| `code.md` | Κώδικας διαλέξεων |
| `samples/` | Υποδειγματικές υποβολές φοιτητών και φοιτητριών |
| `exams/YYYY/` | Θέματα εξετάσεων (PDF και, για το 2023, ανά άσκηση) |
| `past/YYYY/` | Στιγμιότυπα προηγούμενων ετών |
| `assets/pdf/` | Διαφάνειες διαλέξεων και εκφωνήσεις εργασιών |
| `_layouts/`, `_includes/` | Η παρουσίαση — δείτε «Παρουσίαση και θέμα» |

## Τοπική προεπισκόπηση

Χρειάζεστε μόνο `docker`:

```sh
printf 'source "https://rubygems.org"\ngem "github-pages", group: :jekyll_plugins\n' > Gemfile
docker run --rm -v "$PWD:/srv/jekyll" -p 4000:4000 -w /srv/jekyll ruby:3.3-slim \
  sh -c "apt-get update -qq && apt-get install -y -qq git build-essential \
         && bundle install && bundle exec jekyll serve --host 0.0.0.0"
```

Η σελίδα σερβίρεται στο <http://localhost:4000>. Το `github-pages` gem είναι ακριβώς
το σύνολο που τρέχει και στο GitHub Pages, οπότε ό,τι δείτε τοπικά είναι αυτό που θα
δημοσιευτεί. Το `Gemfile` δεν μπαίνει σε commit.

Δεν είναι απαραίτητο για μια διόρθωση κειμένου — για αλλαγές στα `_layouts/` ή στο
`_includes/head-custom.html` όμως, δείτε την πρώτα τοπικά.

## Παρουσίαση και θέμα

Η αισθητική είναι κοινή με το `lab-material` και συντηρείται σε **δύο** αρχεία:

- `_includes/head-custom.html` — η παλέτα (`--dit-cyan`, `--dit-charcoal`,
  `--uoa-blue`), η τυπογραφία, το MathJax και το Mermaid.
- `_layouts/default.html` — τα εμβλήματα, το masthead και το footer. Ο wrapper
  `container-lg px-3 my-5 markdown-body` είναι ακριβώς αυτός που χρησιμοποιεί το
  stock layout του Primer — μην τον αλλάξετε, πάνω του στηρίζεται το κοινό CSS.

Και οι δύο ιστότοποι χρησιμοποιούν το **ίδιο** theme (`jekyll-theme-primer`), οπότε
το `_includes/head-custom.html` είναι κοινό αρχείο και όχι δύο παραλλαγές του:

- Το τμήμα ανάμεσα στους δείκτες `SHARED BEGIN` / `SHARED END` προορίζεται να είναι
  **πανομοιότυπο** με το αντίστοιχο του `lab-material`. Αν αλλάξετε χρώμα, script ή
  κανόνα εκεί μέσα, αντιγράψτε ολόκληρο το μπλοκ και στο άλλο repository.
- Ό,τι βρίσκεται μετά το `SHARED END` αφορά μόνο αυτόν τον ιστότοπο (masthead,
  montage, περιεχόμενα, πλοήγηση ασκήσεων) και δεν αντιγράφεται.

Τα ίδια χρώματα χρησιμοποιεί και το `labs/header.tex` του `lab-material` για τα PDF,
οπότε μια αλλαγή παλέτας αγγίζει τρία σημεία συνολικά.

### Το montage της αρχικής

Η μπάντα με τις εργασίες φοιτητών/τριών είναι **μία εικόνα ανά χρονιά**, σε σειρά
(`.montage-row`). Για να προσθέσετε την περσινή δουλειά: βάλτε το νέο PNG στο
`assets/img/`, προσθέστε ένα `<img>` στο `_layouts/default.html` και ένα
`flex-grow` ίσο με το πλάτος του στο `_includes/head-custom.html`. Οι παλιές εικόνες
δεν ξαναγράφονται.

Ο πίνακας του Go (`assets/img/montage-25.webp`) είναι **παραγόμενη** εικόνα. Το script
που την έφτιαξε δεν φυλάσσεται εδώ — το αποθετήριο είναι περιεχόμενο, όχι κώδικας — αλλά
η εικόνα αναπαράγεται από τις εξής παραμέτρους:

* Ταμπλό 19x19, 140 πέτρες, seed `20260922`, έξοδος 460x460 lossless WebP (render στο 2x
  και σμίκρυνση με Lanczos).
* Η θέση προκύπτει από παρτίδα με **νόμιμες** κινήσεις: αιχμαλωσία, απαγόρευση
  αυτοκτονίας, απλό ko, άνοιγμα στις γωνίες και βαρύτητα προς τις υπάρχουσες πέτρες ώστε
  να σχηματίζονται ομάδες. Καμία ομάδα δεν μένει χωρίς ελευθερίες στην τελική θέση.
* Παλέτα δειγματοληπτημένη από τη σκακιέρα του montage: ταμπλό `#e8c79a`, γραμμές
  `#6b4a2f`, μαύρες πέτρες `#1a1a1a`, άσπρες `#f7f4ee`, πλαίσιο 9px.

Η θέση είναι **ενδεικτική** — δεν είναι παρτίδα που έπαιξε κάποια ομάδα, γιατί η arena
δημοσιεύει μόνο μετα-δεδομένα και όχι κινήσεις.

## Σειρά των ενοτήτων

Η σειρά στο `README.md` δεν είναι τυχαία και **δεν** είναι η σειρά με την οποία
προστέθηκαν τα πράγματα. Ακολουθεί το πόσο συχνά διαβάζεται η κάθε ενότητα. Τρεις ζώνες:

1. **Προσανατολισμός** — `Πρόγραμμα`, `Συμμετοχή`, `Επικοινωνία`. Διαβάζονται μία φορά,
   αλλά αυτή η φορά είναι η πρώτη εβδομάδα, και η `Συμμετοχή` περιέχει την προειδοποίηση
   ότι χωρίς τη φόρμα δεν μπαίνει βαθμός. Είκοσι τρεις γραμμές συνολικά — φθηνές.
2. **Η δουλειά της εβδομάδας** — `Εργασίες`, `Διαλέξεις`, `Εργαστήρια`, στη σειρά και
   **χωρίς τίποτα ανάμεσά τους**. Οι `Εργασίες` προηγούνται αν και οι διαφάνειες
   ανοίγονται συχνότερα: είναι εννέα γραμμές, και είναι το μόνο σημείο όπου η
   καθυστέρηση κοστίζει μονάδες.
3. **Αναφορά, μετά αρχείο** — `Βαθμολογία`, `Κώδικας Τιμής`, `Συγγράμματα`, το υλικό από
   φοιτητές· έπειτα `Προηγούμενα Διαγωνίσματα` (πρώτα στο αρχείο, γιατί ζητούνται πολύ
   πριν την εξεταστική), `Προηγούμενα Έτη`, `Η Ομάδα`, και τα τυπικά στο τέλος.

Μια νέα ενότητα μπαίνει στη ζώνη που της ταιριάζει, όχι στο τέλος του αρχείου. Αν κάτι
διαβάζεται κάθε εβδομάδα, ανήκει στη ζώνη 2 — και τότε κάτι άλλο πρέπει να φύγει από
εκεί, γιατί η αξία της ζώνης είναι ότι είναι μικρή.

Τα anchors παράγονται από το κείμενο των επικεφαλίδων, όχι από τη θέση τους, οπότε η
αναδιάταξη δεν χαλάει υπάρχοντες συνδέσμους. Η μετονομασία μιας επικεφαλίδας τα χαλάει —
προσοχή, τα `quicklinks` (παρακάτω) δείχνουν σε τέσσερα από αυτά.

### Ο προϋπολογισμός της αρχικής στο κινητό

Πάνω από την πρώτη ενότητα υπάρχουν τέσσερα πράγματα: τα εμβλήματα, το masthead, το
montage και ο πίνακας περιεχομένων. Στις 390px πλάτος όλα μαζί έφταναν περίπου τις 1.100px
— μιάμιση οθόνη πριν από οτιδήποτε χρήσιμο. Γι' αυτό:

* Το `montage` **δεν** στοιβάζεται στο κινητό. Δίπλα-δίπλα η σειρά είναι ~105px· στοιβαγμένη
  ήταν 517px. Είναι διακόσμηση, όχι διάγραμμα.
* Ο πίνακας περιεχομένων μένει σε **δύο** στήλες κάτω από 560px (~196px αντί για ~452px).
* Τα `quicklinks` είναι flex και τυλίγονται, ώστε να μη γίνουν ποτέ τρεις σειρές.

Αν προσθέσετε κάτι πάνω από την πρώτη ενότητα, μετρήστε το πρώτα στις 390px. Οι πλατιοί
πίνακες δεν χρειάζονται φροντίδα: το Primer τους δίνει ήδη `overflow: auto`, οπότε
κυλούν οριζόντια αντί να σπάνε τη σελίδα.

## Οδηγίες συγγραφής

- Το κείμενο είναι στα ελληνικά. Κρατάμε το ίδιο ύφος με τα φυλλάδια (β' πληθυντικό,
  φιλικό αλλά διδακτικό) και αφήνουμε στα αγγλικά τα ονόματα μεταβλητών, τις εντολές
  και τις λέξεις-κλειδιά της C.
- Οι σύνδεσμοι προς αρχεία του repository γράφονται σχετικοί (`./assets/pdf/lec00.pdf`)
  ώστε να δουλεύουν και στο github.com και στη σελίδα.
- Τα μαθηματικά γράφονται με `$...$` και αποδίδονται από το MathJax.
- Διαγράμματα γράφονται σε ` ```mermaid ` blocks — αποδίδονται αυτόματα, όπως και στα
  φυλλάδια.
- Οι σελίδες των ασκήσεων στο `exams/2023/fall/exNN/README.md` ξεκινούν με front
  matter (`layout: exercise`, `permalink`, `index`, `prev`, `next`) που παράγει την
  πλοήγηση «Προηγούμενο / Επόμενο». Αν προσθέσετε άσκηση, ενημερώστε τα `prev`/`next`
  των γειτονικών της.

> **Προσοχή:** ένα `README.md` **χωρίς** front matter γίνεται αυτόματα η σελίδα
> ευρετηρίου του καταλόγου του, χάρη στο `jekyll-readme-index`. Ένα `README.md` **με**
> front matter δεν γίνεται — γι' αυτό οι ασκήσεις δηλώνουν ρητό `permalink`.
> Μη λύσετε το πρόβλημα με `readme_index.with_frontmatter: true`: όπως εξηγεί το
> σχόλιο στο `_config.yml`, αυτή η ρύθμιση μετακινεί **όλα** τα υπόλοιπα README έναν
> κατάλογο πιο πάνω (`/past/2023/` → `/past/`).

## Τι δεν δημοσιεύεται

Το `exclude:` στο `_config.yml` κρατά εκτός ιστοσελίδας αρχεία που ζουν στο
repository αλλά δεν είναι υλικό μαθήματος (π.χ. `tas.txt`). Αν προσθέσετε τέτοιο
αρχείο, προσθέστε το και εκεί — οτιδήποτε άλλο αντιγράφεται αυτούσιο στο `_site` και
γίνεται δημόσιο URL.
